<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uhuhuhu.tabtest" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:maxSdkVersion="22"
        android:theme="@style/AppTheme" >
        <activity android:name=".HomeActivity" />
        <activity android:name=".PrescriptionsActivity" />
        <activity android:name=".MeasurementsActivity" />
        <activity android:name=".JournalActivity" />
        <activity android:name=".FeedbackActivity" />
        <activity android:name=".MoreActivity" />
        <activity android:name=".DayActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".ListActivity" />
        <activity android:name=".NewMeasurementActivity" android:noHistory="true"/>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/RemsmedTheme3" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
